#include "lseq_part_propagation_direction.h"

lseq_part_propagation_direction::lseq_part_propagation_direction
  (bool direction, bool validity) {
  this->direction = direction;
  this->validity = validity;
}
